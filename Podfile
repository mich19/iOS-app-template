# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-app-template' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS-app-template
  pod 'Alamofire', '~> 5.2'

  target 'iOS-app-templateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-app-templateUITests' do
    # Pods for testing
  end

end
